# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out

# Build directory - ignore all executables and build artifacts
bin/*.exe
bin/*.exe~
bin/piper-server*
bin/pejelagarto-translator
bin/main.wasm
bin/translator.wasm
bin/wasm_exec.js
bin/checksums*.txt

# Go workspace file
go.work
go.work.sum

# IDE specific files
.vscode/
.idea/
*.swp
*.swo
*~

# OS specific files
.DS_Store
Thumbs.db

# TTS Requirements - All files regenerated by get-requirements.ps1 at runtime
# The PowerShell script is embedded in the binary and downloads dependencies on first run
tts/requirements/

# Coverage output
coverage